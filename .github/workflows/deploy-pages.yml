name: Deploy SDG data cards to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy existing data-card site
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6

      - name: Confirm site entry point exists
        run: test -f output/data_cards/index.html

      - name: Upload data-card site
        uses: actions/upload-pages-artifact@v5
        with:
          path: output/data_cards

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
